class Solution(object):
    def fact(self,num):
        if num<=1:
            return 1
        return self.fact(num-1)*(num)
    def numTrees(self, n):
        return (self.fact(2*n)//(self.fact(n+1)*self.fact(n)))

        
        
        
